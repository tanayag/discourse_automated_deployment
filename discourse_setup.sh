# edited discourse docker repo, with custom configurations
sudo git clone https://github.com/tanayag/discourse_docker.git /var/discourse

cd /var/discourse
sudo chmod 700 containers
sudo bash discourse-setup